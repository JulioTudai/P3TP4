package ProgramacionIII.TP5;

import java.awt.*;

public class TP5EJ6 {
/*
    enum Color {
        VERDE,
        MARRON
    }

    private Color[][] jardin;
    private int n;

    public TP5EJ6(int n) {
        this.n = n;
        this.jardin = new Color[n][n];
        inicializarJardin();
    }

    private void inicializarJardin() {
        // Inicializa toda la matriz con pasto (VERDE)
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                jardin[i][j] = Color.VERDE;
            }
        }
    }

    public void galope() {
        int i = 0;
        int j = 0;
        Color[][] camino = new Color[this.n][this.n];

        arreCaballito(i, j, camino);

        imprimirJardin(camino, this.n);
    }

    public void arreCaballito(int i, int j, Color[][] camino) {

       if (esSolucion(i, j, camino)) {


        /}
        else {
            if (j + 1 <= this.n && jardin[i][j + 1] == Color.VERDE) {
                jardin[i][j + 1] = Color.MARRON;
                arreCaballito(i, j + 1, this.jardin);
                jardin[i][j + 1] = Color.VERDE;
            }
            if (j - 1 >= this.n && jardin[i][j - 1] == Color.VERDE) {
                jardin[i][j - 1] = Color.MARRON;
                arreCaballito(i, j - 1, this.jardin);
                jardin[i][j - 1] = Color.VERDE;
            }
            if (i + 1 <= this.n && jardin[i][i + 1] == Color.VERDE) {
                jardin[i + 1][j] = Color.MARRON;
                arreCaballito(i + 1, j, this.jardin);
                jardin[i + 1][j] = Color.VERDE;
            }
            if (i - 1 >= this.n && jardin[i][i + 1] == Color.VERDE) {
                jardin[i - 1][j] = Color.MARRON;
                arreCaballito(i - 1, j, this.jardin);
                jardin[i - 1][j] = Color.VERDE;
            }
        }


    }




    private void imprimirJardin(Color[][] camino, int n) {
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++) {
                System.out.print((camino[i][j] == Color.VERDE ? "V" : "M") + " ");
            }
            System.out.println();
        }

    }

        */


}





